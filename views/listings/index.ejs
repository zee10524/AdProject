<% layout('layouts/boilerplate') %>
<body>

<div class="categories">
  <span class="category">🔥 Trending</span>
  <span class="category">🏠 Villas</span>
  <span class="category">🛏 Rooms</span>
  <span class="category">💰 Budget Stays</span>
  <span class="category">✨ Luxury</span>
  <span class="category">🏖 Beach</span>
  <span class="category">🏔 Mountains</span>
</div>
<br>

<div class="container mt-4">
  <div class="row g-4 justify-content-center">
    <% for(let listing of allListings){ %>

      <div class="col-lg-4 col-md-6 col-sm-12">
        <div class="listing-card">
          <a href="/listings/<%= listing._id %>" class="listing-link">

            <img src="<%= listing.image.url %>" 
                 alt="<%= listing.title %>">

            <div class="card-info">
              <h5><%= listing.title %></h5>
              <p>₹ <%= listing.price %>/night</p>
              <p class="location">
                📍 <%= listing.location %>, <%= listing.country %>
              </p>
            </div>
          </a>
        </div>
      </div>
    <% } %>
  </div>
</div>
</body>
</html>


